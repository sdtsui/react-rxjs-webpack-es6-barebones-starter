import React, { Component, PropTypes } from 'react';

class StatefulObject extends Component {
  render() {
    const JSX = (
        <div>
          <span>Key:</span>
          <div>State{}</div>
        </div>
    );
    return JSX;
  }
}

export default StatefulObject;
